-- Git integration plugins
return {
  -- Git commands and status
  {
    "tpope/vim-fugitive",
    config = function()
      -- Fugitive will provide the statusline function used in the original config
    end,
  },
}